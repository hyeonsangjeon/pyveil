version: 2
updates:
  # Keep the pinned GitHub Actions commit SHAs current with verified releases.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    labels:
      - dependencies
      - github-actions
